using OpenQA.Selenium;

namespace SpecflowCore.Tests.POM
{
    /// <summary>
    /// Provides HomePage-specific actions and validations.
    /// </summary>
    public static class HomePageActions
    {
        // Currently empty as we're using base actions
        // Add page-specific actions here when needed
    }
}
